server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///${CONFIG_REPO_PATH}  # local git repo for now
          default-label: main
          clone-on-start: true